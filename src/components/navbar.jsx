import React from 'react'
import styles from './navbar.css';

export default () => <div className={ styles.navbar }>
  <a href="#" className={ styles.navlink }>programs</a>
  <a href="#" className={ styles.navlink }>my schedule</a>
  <a href="#" className={ styles.navlink }>profile</a>
</div>
